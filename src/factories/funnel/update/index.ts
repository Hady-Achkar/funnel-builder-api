// Placeholder for funnel update payload factories
// Example structure for future updates:

/*
import { UpdateFunnelPayload } from "../../../types/funnel/update";

export const updateFunnelPayloadFactory = (
  name?: string,
  status?: $Enums.FunnelStatus,
  // ... other optional fields
): Partial<UpdateFunnelPayload> => ({
  ...(name && { name }),
  ...(status && { status }),
  // ... other conditional fields
});

export const funnelUpdatePayloads = {
  funnel: updateFunnelPayloadFactory,
  // ... other update factories
};
*/